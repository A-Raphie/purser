# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Node
node_modules/
.next/
dist/
panel/out/

# Secrets & keys — NEVER commit
.env
.env.*
*.key
wallet.json
credentials.json

# Sibyl local DB (regenerated per environment; session DBs are copied to eval-kit logs deliberately)
*.db
*.db-journal
*.db-shm
*.db-wal

# runtime artifacts (regenerated by scripts)
runtime/*.json
runtime/*.db*

# macOS
.DS_Store

# Demo recording scratch
recordings/scratch/
.venv-acp/
