# Purser runtime deps (Python 3.10+). Judged path: fresh venv + this file.
sibyl-memory-client>=0.7,<0.8
sibyl-memory-cli>=0.3.7,<0.4
sibyl-memory-mcp>=0.1.14,<0.2
x402[evm,httpx]>=2.20,<3
eth-account>=0.14
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27

# dev/test
pytest>=8
